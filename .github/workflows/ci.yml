name: CI

on:
  push:
    branches:
      - main
      - develop
      - feat/**
  pull_request:
    branches:
      - main
      - develop
      - feat/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      # 3️⃣ Setup PNPM (primary)
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10
          run_install: false

      # 3.1️⃣ Ensure pnpm is available (fallback)
      - name: Ensure pnpm is available (fallback)
        run: npm install -g pnpm

      # 4️⃣ Cache PNPM store (optional, speeds up installs)
      - name: Cache PNPM store
        uses: actions/cache@v3
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      # 5️⃣ Install dependencies
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 6️⃣ Typecheck (matches Husky pre-commit)
      - name: Typecheck
        run: pnpm typecheck

      # 7️⃣ Lint
      - name: Lint
        run: pnpm -w lint

      # 8️⃣ Run Tests
      - name: Run Tests
        run: pnpm -w test

      # 9️⃣ Build
      - name: Build
        run: pnpm -w build
