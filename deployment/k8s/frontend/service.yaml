apiVersion: v1
kind: Service
metadata:
  name: whiteboard-client
  namespace: whiteboard-production
  labels:
    app: whiteboard-client
    app.kubernetes.io/component: frontend-client
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: whiteboard-client
